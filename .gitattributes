# Force LF line endings for shell scripts (critical for Docker Linux containers)
*.sh text eol=lf
litestream-wrapper.sh text eol=lf

# Dockerfile should also use LF
Dockerfile text eol=lf

# YAML configs
*.yml text eol=lf
*.yaml text eol=lf
