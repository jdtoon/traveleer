@model IReadOnlyList<saas.Modules.Settings.DTOs.SupplierListItemDto>
@{
    var slug = ViewContext.RouteData.Values["slug"]?.ToString();
}
@if (Model.Count == 0)
{
    <div class="rounded-box border border-dashed border-base-300 bg-base-200/40 p-8 text-center">
        <h3 class="text-lg font-semibold">No suppliers yet</h3>
        <p class="mt-2 opacity-70">Capture your hotel, transfer, excursion, and visa partners here.</p>
    </div>
}
else
{
    <div class="overflow-x-auto">
        <table class="table table-zebra">
            <thead><tr><th>Name</th><th>Contact</th><th>Email</th><th>Phone</th><th>Status</th><th></th></tr></thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr class="hover">
                    <td class="font-medium">@item.Name</td>
                    <td>@(item.ContactName ?? "-")</td>
                    <td>@(item.ContactEmail ?? "-")</td>
                    <td>@(item.ContactPhone ?? "-")</td>
                    <td><span class="badge @(item.IsActive ? "badge-success" : "badge-ghost") badge-sm">@(item.IsActive ? "Active" : "Inactive")</span></td>
                    <td class="text-right">
                        <has-permission name="masterdata.edit">
                            <div class="dropdown dropdown-end">
                                <label tabindex="0" class="btn btn-ghost btn-xs">...</label>
                                <ul tabindex="0" class="dropdown-content menu menu-sm bg-base-200 rounded-box shadow-lg z-10 w-40">
                                    <li><button hx-get="@Url.Action("EditSupplier", "Settings", new { slug, id = item.Id })" hx-target="#modal-container">Edit</button></li>
                                    <li><button class="text-error" hx-get="@Url.Action("DeleteSupplierConfirm", "Settings", new { slug, id = item.Id })" hx-target="#modal-container">Delete</button></li>
                                </ul>
                            </div>
                        </has-permission>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
}
